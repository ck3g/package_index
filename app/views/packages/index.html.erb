<h1>Available Packages</h1>

<ul>
  <%- @packages.each do |package| %>
    <li id="<%= dom_id(package) %>">
      <%= link_to "#{package.name} #{package.version}", package.url %>
      | <%= link_to "Details", package %>
    </li>
  <% end %>
</ul>
